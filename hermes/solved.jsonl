{"id": "a1f3c07", "ts": "2026-07-07T23:10:00", "problem": "PowerShell 5.1 rejects && and || pipeline chain operators with a parser error", "solution": "Use 'A; if ($?) { B }' to run B only if A succeeds, or 'A; B' to chain unconditionally. Ternary and ?? are also unavailable in 5.1 - use if/else.", "tags": ["powershell", "windows", "shell"], "source": "seed:windows-dev", "stale": false}
{"id": "b2e4d18", "ts": "2026-07-07T23:11:00", "problem": "Claude Code PreToolUse hook needs to block a tool call and explain why", "solution": "Exit code 2 from the hook blocks the call; whatever the hook wrote to stderr is fed back to Claude as the explanation. Exit 0 allows. Hook receives tool_name/tool_input as JSON on stdin.", "tags": ["claude-code", "hooks", "guardrails"], "source": "seed:aios-build", "stale": false}
{"id": "c9d5a2f", "ts": "2026-07-07T23:12:00", "problem": "python http.server serves cached/stale files when a dashboard polls JSON state", "solution": "Subclass SimpleHTTPRequestHandler and send Cache-Control: no-store in end_headers(); also fetch with {cache:'no-store'} client-side. Then polling always sees fresh state.", "tags": ["python", "http", "dashboard", "polling"], "source": "seed:aios-build", "stale": false}
{"id": "b085a5e", "ts": "2026-07-07T23:58:34", "problem": "bash heredoc/echo mangles backslashes in JSON test payloads on Windows git bash", "solution": "Never pipe hand-quoted JSON with backslash paths through bash on Windows - escapes collapse and json.load fails silently. Build payloads in Python with json.dumps and subprocess.run(input=...), like .claude/hooks/test_guard.py does.", "tags": ["windows", "bash", "json", "testing"], "source": "aios-build:layer2", "stale": false}
{"id": "8104f24", "ts": "2026-07-08T00:08:35", "problem": "Claude Code hook shows no spawn event when conductor hires a subagent", "solution": "The subagent tool is named Agent in Claude Code 2.x (Task in older builds), and PostToolUse fires only at completion. Emit the spawn from PreToolUse and match tool_name in ('Task','Agent') - see .claude/hooks/mirror.py", "tags": ["claude-code", "hooks", "subagents", "dashboard"], "source": "aios-build:layer4", "stale": false}
{"id": "086cf17", "ts": "2026-07-08T00:22:15", "problem": "git bash on Windows rewrites CLI args that start with a slash into C:/Program Files/Git/... paths", "solution": "MSYS path conversion mangles arguments like /webdesign. Pass them double-slashed (//webdesign), set MSYS_NO_PATHCONV=1, or do the write in Python instead of the shell. Sibling trap to the backslash-JSON one.", "tags": ["windows", "bash", "msys", "cli"], "source": "ember-build:skills", "stale": false}
{"id": "1b84e44", "ts": "2026-07-08T01:08:30", "problem": "focus a specific spawned Claude Code terminal window from a background server on Windows", "solution": "Launch each instance via conhost.exe (cmd /k ... claude) so the conhost process OWNS the window; store that pid; to focus, EnumWindows + GetWindowThreadProcessId==pid then SwitchToThisWindow. Title-matching fails because Claude rewrites the console title after launch. See dashboard/serve.py focus_by().", "tags": ["windows", "ctypes", "win32", "claude-code", "dashboard"], "source": "ember-build:v2", "stale": false}
{"id": "1a60c33", "ts": "2026-07-08T09:52:22", "problem": "dashboard layout blows out horizontally on user's display and 'nothing shows up'", "solution": "Grid/flex children default to min-width:auto, so any nowrap text (mono event strings) forces the track wider than the viewport and pushes columns off-screen. Fix: .grid>*{min-width:0} on every grid/flex child that holds text, overflow-x:clip on main, and ALWAYS verify at ~1280px CSS viewport (Daniel's effective width at his display scaling), not just 1600.", "tags": ["css", "layout", "grid", "dashboard", "windows"], "source": "maestro-build:v3", "stale": false}
{"id": "11f8fcc", "ts": "2026-07-08T11:23:43", "problem": "claude -p critic in an agent loop returns empty result / error_max_turns", "solution": "A -p one-shot judge WILL try tools and blow --max-turns 1, returning subtype error_max_turns with NO result field. Fix: --disallowedTools * plus max-turns 2 and an explicit do-not-use-tools line in the prompt. Treat worker error_max_turns as a partial turn (resume next round), not a crash.", "tags": ["orchestration", "claude-cli", "critic"], "source": "dashboard/orchestrator.py", "stale": false}
{"id": "26bcd4e", "ts": "2026-07-08T11:23:43", "problem": "ssh host 'command' says claude command not found though interactive login works", "solution": "ssh with a command runs a NON-login shell: ~/.profile and nvm never load, so anything in ~/.local/bin or npm/nvm bin is missing from PATH. Prefix the remote command with sourcing /etc/profile ~/.profile ~/.bashrc nvm.sh and export PATH=~/.local/bin:...:PATH. No double quotes if it rides inside cmd.exe quotes.", "tags": ["ssh", "path", "remote"], "source": "dashboard/serve.py", "stale": false}
{"id": "3864751", "ts": "2026-07-08T11:23:56", "problem": "store an ssh password safely on Windows with zero dependencies and feed it to ssh automatically", "solution": "DPAPI via ctypes crypt32 CryptProtectData/CryptUnprotectData (user scope - only this Windows account decrypts), base64 in a gitignored json. Feed to ssh with SSH_ASKPASS_REQUIRE=force + SSH_ASKPASS=.cmd shim that runs a python helper; helper answers yes to host-key prompts and prints the password otherwise. Degrades gracefully to typing in the window.", "tags": ["ssh", "dpapi", "credentials", "windows"], "source": "dashboard/askpass.py", "stale": false}
{"id": "4cbeec1", "ts": "2026-07-08T11:23:56", "problem": "dashboard instance cards and wire sessions are two unlinked lists for the same thing", "solution": "Pass MAESTRO_SID=<sid> in the spawned process env; hook scripts inherit env, so mirror.py prefers os.environ MAESTRO_SID over the hook session_id. Wire events then land under the dashboard's own instance id and cards can show the live stage rail/agents/last action. One env var unifies instances, sessions, and orchestrations.", "tags": ["wire", "env", "sessions", "unification"], "source": ".claude/hooks/mirror.py", "stale": false}
{"id": "ee7f9ea", "ts": "2026-07-08T11:24:07", "problem": "force-directed graph freezes dead on hover", "solution": "Don't gate physics() on hover - run it every frame with a speed factor (drag 1, hover ~0.22, idle 1) applied to the position integration step, and drop the random jiggle only while hovering. Add neighbor highlight from a prebuilt adjacency: fade non-neighbors to 0.16, tint connected edges, with a .18s css opacity transition.", "tags": ["graph", "physics", "hover", "svg"], "source": "dashboard/index.html", "stale": false}
{"id": "5a2f252", "ts": "2026-07-08T11:24:07", "problem": "expanded/selected UI state flashes back on a polling dashboard", "solution": "Any innerHTML re-render wipes DOM state. Key each expandable element (data-k) and keep a Set of open keys in JS; render reapplies the open class from the Set. Same pattern for any toggle that must survive a poll loop.", "tags": ["ui", "polling", "state"], "source": "dashboard/index.html", "stale": false}
{"id": "2459cf8", "ts": "2026-07-08T11:50:19", "problem": "show Claude Code plan usage and time to the 5h reset on a local dashboard", "solution": "No public usage API for subscription accounts - approximate locally: scan ~/.claude/projects/**/*.jsonl with mtime inside the window, sum message.usage tokens for entries whose timestamp is within the last 5h; window start = earliest such timestamp, reset = start+5h. Limits are per-plan and unpublished, so keep them as user config, not hardcoded.", "tags": ["claude-code", "usage", "pulse"], "source": "dashboard/pulse.py", "stale": false}
{"id": "4409890", "ts": "2026-07-08T15:49:27", "problem": "track a Claude account 5h/7d usage + reset accurately when accounts are swapped in one terminal", "solution": "Transcripts under a config dir do NOT record which account sent each message, so scanning them mis-attributes usage after a /login swap. Instead read the true server-side window: capture each account OAuth token from <config_dir>/.credentials.json claudeAiOauth.accessToken (email/uuid from <dir>/.claude.json oauthAccount), send a 1-token POST /v1/messages with Authorization: Bearer <token> + header anthropic-beta: oauth-2025-04-20, and read response headers anthropic-ratelimit-unified-5h-{reset,utilization} and -7d-. count_tokens returns NO ratelimit headers; a 429 (window exhausted) STILL carries them. Cache/throttle ~180s. Store swapped-through accounts in a gitignored seen-cache keyed by accountUuid so a one-terminal workflow accumulates all accounts.", "tags": ["claude-code", "usage", "oauth", "ratelimit"], "source": "dashboard/pulse.py", "stale": false}
{"id": "276b59b", "ts": "2026-07-08T15:49:27", "problem": "python http.server dashboard: new routes 404 intermittently after code changes", "solution": "Stale server processes accumulate bound to the same port (SocketServer allow_reuse_address lets a second bind succeed); requests round-robin across old+new listeners so newly added routes 404 randomly. Kill ALL pids on the port before restarting: netstat -ano | grep 127.0.0.1:PORT.*LISTEN -> taskkill /F each unique pid, then start exactly one.", "tags": ["windows", "http-server", "dashboard"], "source": "dashboard/serve.py", "stale": false}
{"id": "775b4a7", "ts": "2026-07-08T15:49:27", "problem": "carousel arrow buttons do not scroll a CSS scroll-snap container", "solution": "element.scrollTo({left,behavior:smooth}) is a no-op / overshoots to the end on an overflow-x:auto element with scroll-snap-type set. Fix: animate scrollLeft yourself with a requestAnimationFrame easeOutCubic tween while temporarily setting style.scrollSnapType=none, then restore it so drag-snapping still works. Also: a 2.5s poll that rewrites the track innerHTML resets scrollLeft and detaches buttons mid-click - guard the rebuild behind a content-signature (only re-render when the HTML actually changed).", "tags": ["frontend", "scroll-snap", "dashboard"], "source": "dashboard/index.html", "stale": false}
{"id": "485fe0d", "ts": "2026-07-09T09:44:25", "problem": "SSH from the dashboard fails: 'X is not recognized' x3 then Permission denied (publickey,password)", "solution": "Win32-OpenSSH runs the SSH_ASKPASS helper WITHOUT quoting its path, so a space in the path (repo under 'Python Env') makes CreateProcess treat everything before the space as the exe -> 'is not recognized', askpass returns no password, ssh denies. Fix: set SSH_ASKPASS to the Windows 8.3 SHORT path via GetShortPathNameW (PYTHON~1, no spaces); fall back to the full path if 8.3 names are disabled. cwd=ROOT and the remote command are unaffected (CreateProcess handles spaces there).", "tags": ["ssh", "windows", "askpass"], "source": "dashboard/serve.py", "stale": false}
{"id": "d8ebbd8", "ts": "2026-07-09T11:12:44", "problem": "soul directory md file count", "solution": "Found 2 .md files in soul/ directory: CHANGELOG.md, soul.md", "tags": ["mission"], "source": "conductor", "stale": false}
{"id": "339b8b7", "ts": "2026-07-09T11:16:02", "problem": "Sonnet structured output (output_config json_schema) intermittently returns malformed/truncated JSON", "solution": "Adaptive thinking is ON by default on Sonnet 5 and its thinking tokens COUNT AGAINST max_tokens; a 1200 cap left ~200 tokens for the JSON and truncated it mid-brief. Fix: raise max_tokens (3500), slice text[find('{'):rfind('}')+1] before json.loads, and report stop_reason==max_tokens as a distinct error.", "tags": ["anthropic-api", "structured-output", "sonnet"], "source": "dashboard/mission.py", "stale": false}
{"id": "fa315ac", "ts": "2026-07-09T11:16:02", "problem": "orchestrator auto account routing picked an account that immediately failed with weekly limit hit", "solution": "Claude accounts have TWO unified windows (5h and 7d). Routing on 5h utilization alone picks accounts at 0%/5h but 100%/7d which reject work. least_used must score max(pct5h, pct7d) from the anthropic-ratelimit-unified-* headers.", "tags": ["claude-code", "ratelimit", "routing"], "source": "dashboard/pulse.py", "stale": false}
{"id": "6b46041", "ts": "2026-07-09T14:58:24", "problem": "Seek bar jumps backwards during playback due to stale server progress timestamps", "solution": "Use local ticker to advance playback progress between polls; resync seek bar to server value only on actual track/pause state changes, not on every poll. This prevents snapping backwards when stale progress_ms values arrive from the server.", "tags": ["playback", "seek-bar", "state-sync", "spotify-api", "race-condition"], "source": "auto-learn", "stale": false}
{"id": "86eda23", "ts": "2026-07-09T15:00:14", "problem": "Brain learning not capturing solutions — read path exists but write path is manual", "solution": "Register a Stop hook in ~/.claude/settings.json that runs after each turn: reads transcript, uses Haiku to judge if learning is durable (root causes/gotchas/recipes, not chatter), dedupes against Hermes (≥0.75 similarity), writes to solved.jsonl + mirrors to vault + emits learn event. Closes the write loop so every session auto-feeds the brain.", "tags": ["brain", "learning", "hooks", "deduplication", "auto-capture"], "source": "auto-learn", "stale": false}
{"id": "c5452dc", "ts": "2026-07-09T16:21:59", "problem": "Dashboard revamp needed more motion and visual density without AI slop", "solution": "Kept the vanilla dashboard and royal plum/paper theme, added a visible desktop task rail, live ops visual stage, mission stack ring, route panning, scroll-trigger reveal, pointer tilt card motion, and morphing active-run panel. Verified with Playwright desktop/mobile screenshots and a console-clean smoke check.", "tags": ["dashboard"], "source": "dashboard/index.html", "stale": false}
{"id": "5e32c88", "ts": "2026-07-09T17:45:26", "problem": "Three.js plane animation jerky; mid-flight keyframe stall and broken quaternion interpolation freezing frames", "solution": "Replace segmented flight path with one continuous eased animation curve; replace broken `slerpQuaternions` call with three.js built-in `Quaternion.slerp()`; reduce damping coefficient (k=3.4) for smoother interpolation; ensure quaternion is travel-aligned and wings-level throughout", "tags": ["three.js", "animation", "quaternion", "easing", "performance"], "source": "auto-learn", "stale": false}
{"id": "6a33405", "ts": "2026-07-10T11:30:12", "problem": "Unitree G1 sim2real dance tracking: policy trained with ≤20ms latency randomization fell after 44s on hardware due to actual sensorimotor latency of 40–80ms (measured 4 independent ways)", "solution": "Root cause is not language/comms overhead (0.16ms) but actuator response + leg-odometry lag inside robot. Fix: retrain with 0–80ms latency domain randomization + root-pos weight 1.0. Python SDK sufficient; C++ would shave only 1–2ms jitter. Domain randomization robustness beats low-level optimization for this constraint.", "tags": ["sim2real", "latency", "domain-randomization", "unitree-g1", "policy-training"], "source": "auto-learn", "stale": false}
{"id": "162eae6", "ts": "2026-07-10T12:02:39", "problem": "Real-time robot control pipeline had undetected jitter in motion retargeting causing visible twitching during fast hand/body coordination moves in live shows", "solution": "Added frame-level temporal smoothing + outlier rejection before velocity clamp in retargeting pipeline, plus jerk metric in verification gate to auto-flag glitchy motions before robot execution. Measure baseline spike stats first, then validate smoothing filter effectiveness over 5-min safe telemetry run before deployment.", "tags": ["motion-quality", "latency-optimization", "real-time-control", "safety-gating", "show-critica"], "source": "auto-learn", "stale": false}
{"id": "c3fe2c2", "ts": "2026-07-10T13:56:26", "problem": "Unitree G1 robot deployment safety on Windows: POSIX signal handling for damping on exit is unverified; bash runbooks incompatible", "solution": "Deploy actual robot commands only from Ubuntu 22.04 (use original laptop with validated tv env + .secrets/, or dual-boot/WSL2). Windows safe for code/training/prep only. Network: wired Ethernet, static IP 192.168.123.2/255.255.255.0. Python env: unitree_sdk2_python + onnxruntime. First contact: read-only mode (--mode read) to validate SDK/network with zero motion risk.", "tags": ["unitree-g1", "deployment", "safety-critical", "signal-handling", "network-config", "environment"], "source": "auto-learn", "stale": false}
{"id": "ae42a38", "ts": "2026-07-10T14:02:35", "problem": "Motion twitching caused by isolated single-frame jerk spikes (5-15× baseline) in all dance CSVs", "solution": "Apply robust z-score outlier rejection (per-joint MAD, z>10, floor 150 rad/s²) with cubic-spline re-interpolation, then Savitzky-Golay smoothing (window 7, polyorder 3) in joint space and quaternion tangent-space (slerp-aware) for root orientation. Wire before velocity clamp in prep_motion pipeline. Verified: spike frames reduced 96-100%, jerk peaks cut 4-20×, sharp choreography preserved (≤0.04 rad RMS fidelity loss).", "tags": ["motion-filtering", "jerk-spikes", "savitzky-golay", "quaternion-smoothing", "robotics-animat"], "source": "auto-learn", "stale": false}
{"id": "487e4b4", "ts": "2026-07-10T14:39:59", "problem": "Preview videos missing from frontend; .gitignore excludes media files (*.mp4, *.npz, data/policies/*) so they exist only on source machine", "solution": "Copy data/previews/, data/policies/, data/jobs/ directories from original machine via USB/scp, or regenerate previews locally from tracked CSV files using repo's MuJoCo renderer. Gitignored media won't clone automatically.", "tags": ["gitignore", "media-serving", "deployment", "cross-machine-sync"], "source": "auto-learn", "stale": false}
{"id": "da0d67f", "ts": "2026-07-10T15:03:49", "problem": "Playwright tests polluted production data by recording fake incidents against live records, silently corrupting their state (demoting show-ready to sim-verified, zeroing clean streaks)", "solution": "Point Playwright tests at throwaway/sandbox records only, never against live library. Audit test setup to isolate test incidents from production state before pushing.", "tags": ["testing", "data-isolation", "playwright", "test-pollution", "production-safety"], "source": "auto-learn", "stale": false}
{"id": "10ffce7", "ts": "2026-07-10T15:24:44", "problem": "Double-counting mechanical lag in sim2real fidelity retrain kills station-keeping", "solution": "Cap curriculum latency at 60 ms (not 80) when sim PD loop already models mechanical lag. Three-stage resume: 0–20 ms (4k iter), 0–50 ms (+3k), 0–60 ms (+3k). Verify at 40 ms+push AND drift <1 m.", "tags": ["sim2real", "curriculum", "latency", "fidelity", "mechanical-lag"], "source": "auto-learn", "stale": false}
{"id": "3ae8745", "ts": "2026-07-10T15:31:01", "problem": "Exam signing key (32 raw bytes) failed validation after text round-trip extraction due to appended newline", "solution": "Strip trailing newline from extracted signing key before use; verify against a committed signed verdict to catch byte-exactness failures early. Store as raw bytes, not text.", "tags": ["credentials", "signing-key", "byte-exactness", "extraction-bug"], "source": "auto-learn", "stale": false}
{"id": "bb2ed38", "ts": "2026-07-10T22:06:07", "problem": "__webpack_modules__[moduleId] is not a function runtime error in Next.js dev server", "solution": "Corrupted .next cache from running `next build` while dev server was active. Delete the .next folder and restart `npm run dev` to regenerate. Rule: never run `next build` concurrently with dev server.", "tags": ["nextjs", "webpack", "cache", "devops", "runtime-error"], "source": "auto-learn", "stale": false}
{"id": "ac0bd9d", "ts": "2026-07-10T22:08:26", "problem": "Next.js dev server returns 500 Internal Server Error on all routes after .next cache deleted", "solution": "Stop the running dev server (Ctrl+C) and restart with `npm run dev`. The live process cannot survive its cache being removed; it must be restarted to rebuild. First page load takes ~15s while recompiling.", "tags": ["nextjs", "dev-server", "cache", "500-error", "troubleshooting"], "source": "auto-learn", "stale": false}
{"id": "d9236fa", "ts": "2026-07-10T22:24:17", "problem": "OpenSky API calls fail silently after OAuth2 migration despite valid credentials in credentials.json", "solution": "Environment variables OPENSKY_CLIENT_ID and OPENSKY_CLIENT_SECRET must be set in .env files (aeolus/.env for Docker, aeolus/apps/web/.env.local for Next dev relay), not read from credentials.json. OpenSky now blocks anonymous/unauthenticated requests. Restart dev server after creating .env files.", "tags": ["opensky", "oauth2", "env-config", "api-auth", "docker-compose"], "source": "auto-learn", "stale": false}
{"id": "62c476d", "ts": "2026-07-13T10:59:34", "problem": "Skill tree UI with horizontal scrolling is hard to navigate; needs clearer hierarchy, dark mode, and concise descriptions", "solution": "Rebuild as vertical spine layout (genres stack top-to-bottom, skills grid-wrap within sections). Add dark-mode toggle in topbar that persists via localStorage (applied before first paint to avoid white flash). Cap skill descriptions to 1–2 sentences in detail panel, with metadata as chips. Keep account cards white in dark mode for brand consistency while re-scoping text colors locally for readability.", "tags": ["ui-layout", "accessibility", "dark-mode", "skill-tree", "responsive-grid"], "source": "auto-learn", "stale": false}
{"id": "58373cb", "ts": "2026-07-13T14:10:32", "problem": "UI maximalism: cursor-tracking 3D card tilt + decorative rings on every card create visual noise; sparse cards stretch to equal height leaving empty voids", "solution": "Remove perspective transform handlers (rotateX/rotateY on pointer-move); replace with scroll entrance + hover lift only. Strip concentric rings from account cards (keep one dark accent card as focal point, not repetition). Set carousel to align-items:flex-start so cards size to content, eliminating voids.", "tags": ["ui-declutter", "css-perspective", "carousel-layout", "visual-hierarchy"], "source": "auto-learn", "stale": false}
{"id": "e8edee1", "ts": "2026-07-13T14:22:35", "problem": "Brain/knowledge base filling with routine noise instead of signal: every completed mission auto-logged, every role told to record, cheap first-try successes polluted the vault", "solution": "Add `_worth_remembering()` gate that writes to brain only on: failure | cost ≥$0.15 | ≥2 roles | opus/fable reasoning | ≥40 turns depth. Named constants (`WORTH_COST`, `WORTH_TURNS`) allow one-line weight tuning. Apply gate in CEO pipeline (mission-level) and orchestrator accept path. Prune existing noise from vault + jsonl, regenerate index.", "tags": ["knowledge-base-signal", "selective-logging", "memory-hygiene", "cost-gating", "thresholds"], "source": "auto-learn", "stale": false}
{"id": "27df10e", "ts": "2026-07-13T14:29:52", "problem": "Split-button dropdown menu with model/effort/stats overrides was stripped from Run button during UI revamp", "solution": "Restore split-button + popover pattern: (1) add override-handling backend in plan_and_start(text, opts) to apply tuning after CEO plans; (2) re-insert CSS for split-button + popover UI; (3) wire JS to pass opts object on send and populate Account list live from configured accounts; (4) add visual indicator (white dot on chevron) when non-Auto overrides are active", "tags": ["ui-regression", "dropdown-menu", "backend-wiring", "form-state", "split-button-pattern"], "source": "auto-learn", "stale": false}
{"id": "82065e1", "ts": "2026-07-13T14:56:58", "problem": "Model client must keep caller loops alive when a model refuses", "solution": "Fable 5 (and Opus/Sonnet) return safety refusals as HTTP 200 with stop_reason='refusal', NOT an error. In model_client.normalize(), collapse every outcome (refusal, HTTP error, truncation, clean answer) into one 200-shaped envelope with a stopped_reason field: None on clean end_turn, else the reason ('refusal'/'http_500'/'max_tokens'). Callers branch on stopped_reason and re-route; they never raise/exit. Also: Fable thinking is always-on so omit the thinking param and control depth via output_config.effort; xhigh is gated behind an agentic-loop flag (else -> high); model swap to Opus is one constant (MODEL=MODEL_IDS['opus']).", "tags": ["mission", "model-client", "refusal"], "source": "mission:model-layer", "stale": false}
{"id": "71284fb", "ts": "2026-07-13T14:57:13", "problem": "Anthropic Fable 5 returns refusals as HTTP 200 with stop_reason='refusal', not as HTTP 4xx errors", "solution": "Intercept refusals by checking `stop_reason == 'refusal'` in the HTTP 200 response body, not by catching exceptions. Normalize all outcomes (refusal, error, truncation, success) into a single 200 envelope with `stopped_reason` field for consistent downstream handling.", "tags": ["anthropic", "fable", "refusal-handling", "http-api", "stop-reason"], "source": "auto-learn", "stale": false}
{"id": "5bc9f2e", "ts": "2026-07-13T15:01:38", "problem": "Cron daily-review loop must be validatable offline and survive a model refusal mid-sweep", "solution": "loop.sh (cron entry): absolute ROOT via BASH_SOURCE, minimal PATH, load .env with set -a, atomic run-lock via mkdir (portable, no flock), once-per-day .last-run stamp for idempotency; --dry-run bypasses both guards. review.py: scan=haiku(cheap)->findings shaped as the task contract; escalate=opus writes A-Z plan, model tag reused from model_client._DELEGATE by complexity. Refusal handling keys on env['stopped_reason'] from normalize(); dry-run injects a refusal by building normalize({stop_reason:'refusal'}) on the 2nd finding so the continue-not-exit path is provable with no API key.", "tags": ["mission"], "source": "conductor", "stale": false}
{"id": "4a92450", "ts": "2026-07-13T15:01:57", "problem": "Model refusals (HTTP 200 + stopped_reason field) must be intercepted mid-loop without hard-exiting the entire pipeline", "solution": "Key on `env['stopped_reason']` from model_client.normalize() after every call. Mark finding 'blocked', log the refusal reason, continue to next item. Refusals are HTTP 200 with stop_reason field, not HTTP errors—normalize() collapses all outcomes (refusal/error/truncation/success) into one 200 envelope.", "tags": ["refusal-handling", "model-client", "error-recovery", "cron-resilience"], "source": "auto-learn", "stale": false}
{"id": "da3988f", "ts": "2026-07-13T15:07:14", "problem": "agent-card status is a whole-record emit: a transition after push_feedback wiped the note", "solution": "set_card writes the FULL card each call, so flipping a role to 'working' right after pushing a feedback note re-emits with note='' and silently erases it. Carry forward fields you must preserve: load_card first, re-pass note=c['note']. Applies to any field not in the transition (cost, stopped_reason too).", "tags": ["mission", "orchestration"], "source": "conductor", "stale": false}
{"id": "baefb05", "ts": "2026-07-13T15:10:43", "problem": "I want you to orchestrate and revamp this system(u will use fable 5) to make it even more automatic (and make it way easier manage and view everything on my end) add a system that ", "solution": "architect=done; review-loop=done; orchestration=done; grading-audit=failed; health-ui=blocked; integrator=blocked. You've hit your session limit · resets 6:50pm (Asia/Bangkok)", "tags": ["mission", "ceo"], "source": "ceo:53b64440", "stale": false}
